// nanopb_utils.cpp

// Include necessary headers if any non-template, non-inline functions
// were added to nanopb_utils.h.
// In this case, based on the provided nanopb_utils.h, all functions are
// templates or inline helpers, so this file might remain empty or just
// include the header for organizational purposes or future additions.

#include "nanopb_utils.h"

// No function bodies needed here if all helpers are inline/templates in the header.